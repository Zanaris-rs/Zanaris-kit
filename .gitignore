node_modules/
dist/

# electron-vite build output
out/

# npm run capture
captures/

# the seeded design canvas: 2.5MB of editor payload, rebuilt by design/build.mjs + seed-canvas
design/swiftkit-interface.html

# the staged engine, its working checkouts and its archive (npm run stage:engine)
engine-dist/
.engine-work/
engine-*.tar.gz

# electron-builder output (npm run dist)
release/

# agent working state: ledgers, review diffs, and a brainstorm session token.
# .superpowers/sdd/ already self-ignores, but brainstorm/ does not, and it holds
# a live token — so ignore the tree rather than each subdirectory as it appears.
.superpowers/
